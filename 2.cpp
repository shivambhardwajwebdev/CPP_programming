#include<iostream>
using namespace std;
int main(){
    cout<<"enter the length and the height of the rectangle : ";
    // int length;
    //int height;
    //declaration the value at same line
    int length,height;
    // cin>>length;
    // cin>>breadth;
    //take input in same line 
    cin>>length>>height;
    cout<<"area is : "<<length*height;
    return 0;
}